services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bitgraph_app
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules # Impede que a pasta node_modules do host sobreponha a do container
    environment:
      - NODE_ENV=development
